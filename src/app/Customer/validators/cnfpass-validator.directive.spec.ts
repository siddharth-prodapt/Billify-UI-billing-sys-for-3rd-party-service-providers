import { CnfpassValidatorDirective } from './cnfpass-validator.directive';

describe('CnfpassValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CnfpassValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
